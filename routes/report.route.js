const express = require("express");
const report_controller = require("../controllers/report.controller");

const router = express.Router();

// router.get("/report", report_controller._get_report);
// router.post("/report", report_controller._post_report);

// router.get("/report-manage", report_controller._get_report_manage)

// router.get("/report/state/:id", report_controller._update_report_state);

module.exports = router;
